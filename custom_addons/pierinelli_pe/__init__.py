# -*- coding: utf-8 -*-
# La configuracion contable (plan de cuentas 'pe', IGV, RUC) y las
# transacciones con IGV se hacen en el seed post-install (seed_pe.py),
# porque el plan de cuentas solo carga de forma fiable con el registry
# ya cargado (fuera del post_init de instalacion).
